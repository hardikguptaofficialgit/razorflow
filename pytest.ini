[pytest]
pythonpath = . agent-backend
markers =
    legacy: V1 agent-loop runtime (unsupported; excluded from release CI)
    asyncio: async tests
testpaths =
    tests/agent
    tests/policy
    tests/voice
    agent-backend/tests
